package com.bacoge.constructionmaterial.config;

/**
 * Deprecated configuration placeholder.
 *
 * NOTE:
 * Beans for security (PasswordEncoder, AuthenticationManager, UserDetailsService, etc.)
 * are now fully defined in SecurityConfig to avoid duplicate bean definitions that
 * prevented application startup.
 *
 * This class is intentionally left without @Configuration and without @Bean methods.
 */

public class SecurityBeansConfig {
    // Intentionally empty.
}
